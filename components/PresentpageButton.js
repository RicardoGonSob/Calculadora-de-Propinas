import { Button, StyleSheet, Text, View, TextInput } from 'react-native';


export default function PresenpageButton( props ){
    return(
        <View>
            <TextButton tittle={props.porcentage}/>
        </View>
    );
}